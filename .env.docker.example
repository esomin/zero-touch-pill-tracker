# ── [백엔드 DB 설정] ──────────────────────────────────────────
# MongoDB Atlas 연결 문자열 (
MONGO_URI=YOUR_MONGO_URI
DB_NAME=med_tracker
BACKEND_PORT=8000

# ── [프론트엔드 빌드 설정] ──────────────────────────────────────
# SSL / HTTPS 도메인 적용 (예: https://api.yourdomain.com 또는 https://yourdomain.com:8000)
VITE_API_URL=https://YOUR_DOMAIN
VITE_WS_URL=wss://YOUR_DOMAIN
# 프론트엔드 포트
FRONTEND_PORT=3002




